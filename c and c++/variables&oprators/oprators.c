//arithmatik oprators
#include <stdio.h>
int main(int argc, char const *argv[])
{
    /* code */
    int a=12;
    float b=72;
    int c=136;
    printf("a+b = %f\n", a+b);
    printf("a-b = %f\n", a-b);
    printf("a*b = %f\n", a*b);
    printf("a/b = %f\n", a/b);
    // some arithmatik oprators are not possible with float .example "%" is c . this oprator gives remainder in c//
    return 0;
}

// relational oprators, istemal hogga in iff, while loops later on.
// bit-wise oprators se vedio