#include <stdio.h>
int main()
{
int a[]={23, 25, 26};
printf("value of a[0] is %d\n", a[0]);
printf("value of a[1] is %d\n", a[1]);
printf("value of a[2] is %d\n", a[2]);
return 0;
}//array intialization in diffrent way